name: CI

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]

permissions:
  contents: read
  checks: write
  pull-requests: write
  security-events: write

jobs:
  code-quality:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 0
    
    - name: Set up Python 3.12
      uses: actions/setup-python@v6
      with:
        python-version: '3.12'
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Lint with Ruff
      run: |
        ruff check app/ tests/
      continue-on-error: true
    
    - name: Format check with Black
      run: |
        black --check app/ tests/
      continue-on-error: true
    
    - name: Lint with Pylint
      run: |
        pylint app/ --exit-zero
      continue-on-error: true

  build-and-test:
    runs-on: ubuntu-latest
    needs: code-quality
    
    steps:
    - uses: actions/checkout@v6
    
    - name: Set up Python 3.12
      uses: actions/setup-python@v6
      with:
        python-version: '3.12'
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run tests with pytest
      env:
        OPENAI_API_KEY: ${{ github.actor == 'dependabot[bot]' && 'sk-test-key-for-dependabot' || secrets.OPENAI_API_KEY }}
        API_SECRET_KEY: ${{ github.actor == 'dependabot[bot]' && 'test-api-secret-key-for-dependabot' || secrets.API_SECRET_KEY }}
        JWT_SECRET_KEY: ${{ github.actor == 'dependabot[bot]' && 'test-jwt-secret-key-for-dependabot' || secrets.JWT_SECRET_KEY }}
        REDIS_ENABLED: false
      run: |
        pytest tests/ -v --cov=app --cov-report=xml --cov-report=term
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v5
      with:
        files: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        token: ${{ secrets.CODECOV_TOKEN }}
        fail_ci_if_error: false
    
    - name: Build Docker image
      run: |
        docker build -t multimodal-analytics:latest .
    
    - name: Test Docker image
      env:
        OPENAI_API_KEY: ${{ github.actor == 'dependabot[bot]' && 'sk-test-key-for-dependabot' || secrets.OPENAI_API_KEY }}
        API_SECRET_KEY: ${{ github.actor == 'dependabot[bot]' && 'test-api-secret-key-for-dependabot' || secrets.API_SECRET_KEY }}
        JWT_SECRET_KEY: ${{ github.actor == 'dependabot[bot]' && 'test-jwt-secret-key-for-dependabot' || secrets.JWT_SECRET_KEY }}
        REDIS_ENABLED: false
      run: |
        docker run --rm -e OPENAI_API_KEY -e API_SECRET_KEY -e JWT_SECRET_KEY -e REDIS_ENABLED multimodal-analytics:latest python -c "from app.main import app; print('OK')"

  security-scan:
    runs-on: ubuntu-latest
    needs: code-quality
    
    steps:
    - uses: actions/checkout@v6
    
    - name: Run Trivy filesystem scan
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'sarif'
        output: 'trivy-results.sarif'
      continue-on-error: true
    
    - name: Upload Trivy results to GitHub Security
      uses: github/codeql-action/upload-sarif@v4
      with:
        sarif_file: 'trivy-results.sarif'
      continue-on-error: true

